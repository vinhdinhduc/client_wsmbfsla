import{notFound}from'next/navigation';import{newsApi}from'@/lib/api/news';import{ArticleBody}from'../../[slug]/_components/ArticleBody';export default async function Preview({params}:{params:Promise<{token:string}>}){const{token}=await params;let news;try{news=await newsApi.preview(token)}catch{notFound()}return <main style={{maxWidth:900,margin:'0 auto',padding:32}}><p>Chế độ xem trước — bài chưa công khai</p><h1>{news.title}</h1><ArticleBody news={news}/></main>}
