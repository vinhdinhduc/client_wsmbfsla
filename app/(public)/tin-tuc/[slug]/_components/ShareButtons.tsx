'use client';import{useState}from'react';export function ShareButtons(){const[copied,setCopied]=useState(false);const url=typeof window==='undefined'?'':window.location.href;return <nav aria-label="Chia sẻ" style={{display:'flex',gap:12}}><a href={`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(url)}`} target="_blank" rel="noopener noreferrer">Facebook</a><a href={`https://zalo.me/share?url=${encodeURIComponent(url)}`} target="_blank" rel="noopener noreferrer">Zalo</a><button type="button" onClick={async()=>{await navigator.clipboard.writeText(location.href);setCopied(true)}}>{copied?'Đã sao chép':'Sao chép liên kết'}</button></nav>}
